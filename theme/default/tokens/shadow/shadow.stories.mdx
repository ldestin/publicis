import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { Grid, Typography, Box } from '@material-ui/core';
import { ThemeProvider } from '../../../../themes';

<Meta title="Core|Shadow" />

# Shadow

<br />

##### Shadow values are used to add space within or around the elements.

[More on Shadow](https://material-ui.com/system/shadows)

<br />
<br />

#### Values

<Preview style={{ backgroundColor: '#f5f5f5' }}>
  <ThemeProvider>
    <Grid container alignItems="center">
      {Array(20)
        .fill()
        .map((item, index) => (
          <Grid item>
            <Box
              boxShadow={index}
              style={{
                textAlign: 'center',
                width: '60px',
                height: '60px',
                color: '#FFF',
                flexShrink: 0,
                margin: '20px',
                backgroundColor: 'rgb(33, 150, 243)',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
              }}
            >
              <Typography variant="h5">{index}</Typography>
            </Box>
          </Grid>
        ))}
    </Grid>
  </ThemeProvider>
</Preview>

#### Usage

`<Box boxShadow={0}>…`
`<Box boxShadow={1}>…`
`<Box boxShadow={2}>…`
`<Box boxShadow={3}>…`
